name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Build
        run: npm run build
        
      - name: Fix static asset paths
        run: |
          # Fix image paths in HTML files to include basePath
          echo "Fixing static asset paths..."
          find out/ -name "*.html" -exec sed -i 's|src="/assets/|src="/zeeautotryes-website/assets/|g' {} \;
          find out/ -name "*.html" -exec sed -i 's|href="/assets/|href="/zeeautotryes-website/assets/|g' {} \;
          find out/ -name "*.html" -exec sed -i 's|data-src="/assets/|data-src="/zeeautotryes-website/assets/|g' {} \;
          find out/ -name "*.html" -exec sed -i 's|data-background="/assets/|data-background="/zeeautotryes-website/assets/|g' {} \;
          echo "Fixed static asset paths in HTML files"
        
      - name: Setup Pages
        uses: actions/configure-pages@v4
        
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
